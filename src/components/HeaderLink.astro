---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { href, class: className, ...props } = Astro.props;
const pathname = Astro.url.pathname.replace(import.meta.env.BASE_URL, "");
const isActive = href === pathname + import.meta.env.BASE_URL.replace("/", "") || href === import.meta.env.BASE_URL + pathname;
---

<a href={href} class:list={[className, { active: isActive }]} {...props}>
	<slot />
</a>
<!-- <p>
	pathname: {pathname} | subpath: {subpath} | href: {href} | check1: {pathname + import.meta.env.BASE_URL.replace("/", "")} | check2: {
		import.meta.env.BASE_URL + "/" + (subpath?.join("") || "")
	}
</p> -->
<style>
	a {
		display: inline-block;
		text-decoration: none;
	}
	a.active {
		font-weight: bolder;
		text-decoration: underline;
	}
</style>
