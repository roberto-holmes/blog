---
// interface Props {
// 	date: Date;
// }

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(Astro.url.pathname.includes("/es/") ? "es" : "en-gb", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
